export default function cardFive({ className }: { className: string }) {
  return (
    <div
      className={`${className} h-screen bg-[#D4AF37] text-black flex items-center justify-center overflow-hidden  text-9xl rounded-xl`}
    >
      <h1>Card Five</h1>
    </div>
  );
}
